[phases.setup]
nixPkgs = ["python310", "python310Packages.pip", "gcc", "libffi", "openssl"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "bash run.sh"
